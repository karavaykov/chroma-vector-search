# GPU acceleration dependencies for Chroma Vector Search
# Install with: pip install -r requirements-gpu.txt

# Base dependencies
chromadb>=0.5.0
sentence-transformers>=2.2.2
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# GPU acceleration
torch>=2.0.0

# For CUDA support on Linux/Windows (choose appropriate version):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# For Apple Silicon (MPS):
# pip install torch torchvision torchaudio

# Optional: For mixed precision training
# pip install accelerate